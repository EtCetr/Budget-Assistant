📋 СВОД ПРАВИЛ ДЛЯ AI-АССИСТЕНТА — BUDGET ASSISTANT v6.0
Передавать в начале каждой новой сессии вместе с `DECISIONS.md` и `PROJECT_CONTEXT.md`.
Составлен по реальным ошибкам Этапа 7 (Transactions Log Screen): 181 ошибка analyze,
~10 итераций правок. Цель — не повторить их.

🔴 КРИТИЧЕСКИЕ ПРАВИЛА (нарушение = баги в коде)

1. ВСЕГДА проверяй содержимое файлов ПЕРЕД генерацией кода
❌ НЕ делай: «Предполагаю, что у тебя есть файл X, вот код для него…»
✅ ДЕЛАЙ: сначала запроси содержимое ОДНОЙ командой в терминале (см. правило 17).
Почему: без реального содержимого ты не знаешь сигнатур, имён полей и структуры.
Генерация вслепую = 5–10 итераций исправлений.

2. Проверяй версии пакетов в pubspec.yaml ПЕРЕД использованием API
❌ Logger('Name')            // сигнатура logger 1.x
✅ Logger()                 // logger 2.x: позиционного имени НЕТ
Частые ловушки:
- logger 2.x vs 1.x — разные сигнатуры конструктора и метода e()
- DropdownButtonFormField: value deprecated → использовать initialValue
- build_runner: флаг --delete-conflicting-outputs УДАЛЁН в новых версиях
- Riverpod 3.x: StateNotifier/StateNotifierProvider УДАЛЕНЫ → Notifier/NotifierProvider
- Freezed 3.x: классы должны быть abstract

3. Получай реальные сигнатуры конструкторов ПЕРЕД вызовом
Команда проверки:
   Select-String -Path lib/.../file.dart -Pattern 'class ClassName(' -Context 10
Пример: конструктор TransactionsRepositoryImpl({required AppDatabase db, required Logger logger})
→ вызывать ТОЛЬКО с именованными: TransactionsRepositoryImpl(db: db, logger: logger)

4. Проверяй наличие файлов ПЕРЕД импортом
Команда проверки:
   Get-ChildItem lib -Recurse -Filter *.dart | Where-Object { $_.Name -like 'имя' }
Если файла нет — не импортируй его: используй существующие провайдеры/репозитории
или спроси пользователя, где лежит нужный файл.

🟠 ВАЖНЫЕ ПРАВИЛА (нарушение = потеря времени)

5. Проверяй кодировку файлов (UTF-8 BOM ломает кодогенерацию)
Симптомы BOM (байты EF BB BF в начале файла):
- freezed/drift_dev падают с «Variables must be declared using…» / «Illegal character»
- «Missing concrete implementations» после генерации
- PowerShell-замены текста не применяются
Проверка:
   $bytes = [System.IO.File]::ReadAllBytes('file.dart')
   if ($bytes[0] -eq 0xEF -and $bytes[1] -eq 0xBB -and $bytes[2] -eq 0xBF) { 'BOM!' }
Лечение:
   $new = $bytes[3..($bytes.Length-1)]; [System.IO.File]::WriteAllBytes('file.dart', $new)
⚠️ При ЛЮБОЙ записи файлов из терминала используй кодировку БЕЗ BOM:
   [System.Text.UTF8Encoding]::new($false)

6. Freezed-классы — ВСЕГДА с ключевым словом abstract
✅ @freezed abstract class X with _$X { const factory X({...}) = _X; }
После правок entity-файлов перегенерируй принудительно:
   dart run build_runner clean
   Get-ChildItem -Path lib -Recurse -Filter *.freezed.dart | Remove-Item -Force
   dart run build_runner build

7. Читай содержимое enum-файлов ПЕРЕД использованием констант
Пример ошибки: SyncStatus.error не существует, реальные значения: pending, synced, conflict.
Команда: Get-Content lib/core/enums/transaction_enums.dart

8. Для русской локали intl добавляй инициализацию в main()
   import 'package:intl/date_symbol_data_local.dart';
   import 'package:intl/intl.dart';
   await initializeDateFormatting();
   Intl.defaultLocale = 'ru';
Без этого DateFormat.MMMEd('ru') падает с LocaleDataException прямо в UseCase.

9. После git commit --amend уже запушенного коммита — объясняй force-push
   git push --force-with-lease origin main
Объясняй причину: amend переписал SHA, remote отклоняет non-fast-forward.
--force-with-lease безопаснее --force (не затрёт чужие коммиты).

10. Проверяй дубли роутов ПЕРЕД добавлением в app_router.dart
   (Select-String -Path lib/core/router/app_router.dart -Pattern "path: '/имя'").Count
Дубль name/path роняет приложение на старте:
«duplication fullpaths for name …» (assert в go_router).

🟡 ПРАВИЛА КОММУНИКАЦИИ И РАБОТЫ С ФАЙЛАМИ

11. Не говори «вставь строку» без точного якоря
Точечные правки допустимы ТОЛЬКО как исключение и только с якорем «найти/заменить».
Стандарт — целые файлы через один скрипт (правило 18).

12. Давай готовые файлы ЦЕЛИКОМ, а не патчи
Пользователь-новичок теряется в точечных правках. Стандартный формат — один скрипт,
который перезаписывает все нужные файлы целиком (правило 18).

13. Признавай свои ошибки сразу, не перекладывай на пользователя
❌ «У тебя неправильная сигнатура, исправь»
✅ «Моя ошибка: в logger 2.x сигнатура другая. Вот исправленный скрипт: …»

14. Спрашивай предпочтения ПЕРЕД генерацией спорной логики
Пример: фильтр суммы по модулю vs по знаку; семантика сегмента «Семейные».
Сначала вопрос с вариантами, потом код выбранного варианта.

15. Объясняй ожидаемое поведение фич ДО теста пользователем
Если действие не даёт видимого эффекта на текущем этапе — скажи об этом заранее:
что запишется в БД, что будет видно сейчас, что появится позже и как отменить.

16. Пользователь НЕ программист — только готовые команды
Пользователь не пишет и не отлаживает код сам. Запрещены формулировки
«вызови», «подключи», «создай где-нибудь», «проверь, что у тебя есть».
Допустимо только:
16.a. Дать ОДНУ готовую команду/скрипт для терминала (правила 17 и 18) + одно
      понятное действие («прикрепи полученный файл в чат», «запусти и пришли вывод»).
16.b. Каждая инструкция обязана содержать ПОЛНЫЙ путь к файлу от корня проекта.
16.c. Все команды — только под Windows PowerShell. Команды сборки/проверки тоже
      давай готовыми одной строкой (`dart run build_runner build`, `flutter analyze`).

17. СБОР КОНТЕКСТА — одна команда, результат в один файл
Когда нужно прочитать существующие файлы, НЕ проси прислать их по одному.
Дай ОДНУ команду, которая собирает всё в один файл для прикрепления в чат.
Шаблон (адаптируй список файлов под задачу):
   $out = 'context.txt'
   Remove-Item $out -ErrorAction SilentlyContinue
   $files = @('lib\путь\файл1.dart', 'lib\путь\файл2.dart')
   foreach ($f in $files) {
     if (Test-Path $f) {
       Add-Content $out "===== FILE: $f =====" -Encoding UTF8
       Get-Content $f | Add-Content $out -Encoding UTF8
       Add-Content $out '' -Encoding UTF8
     } else {
       Add-Content $out "===== MISSING: $f =====" -Encoding UTF8
     }
   }
   Write-Host 'Готово. Прикрепи файл в чат.'
Требования:
- Помечай каждый файл заголовком `===== FILE: путь =====`.
- Помечай отсутствующие файлы `===== MISSING: путь =====`.
- Используй `-Encoding UTF8`.
- В конце — одна фраза, что делать с файлом.
Если файлов ОЧЕНЬ много — допустимо разбить на 2–3 команды по логическим группам,
но стремись к одной.

18. ПРИМЕНЕНИЕ ИЗМЕНЕНИЙ — один скрипт на все файлы
Когда нужно создать или изменить файлы, НЕ давай отдельные инструкции для каждого.
Дай ОДИН скрипт, который создаёт/перезаписывает ВСЁ сразу.
Шаблон:
   $files = @{}
   $files['lib\путь\файл1.dart'] = @'
   содержимое файла 1 целиком
   '@
   $files['lib\путь\файл2.dart'] = @'
   содержимое файла 2 целиком
   '@
   foreach ($entry in $files.GetEnumerator()) {
     $dir = Split-Path $entry.Key -Parent
     if (!(Test-Path $dir)) { New-Item -ItemType Directory -Path $dir -Force | Out-Null }
     [System.IO.File]::WriteAllText($entry.Key, $entry.Value, [System.Text.UTF8Encoding]::new($false))
     Write-Host "Записан: $($entry.Key)"
   }
   Write-Host 'Готово. Теперь запусти: dart run build_runner build; flutter analyze'
Требования:
- Содержимое файлов — в here-строках `@'...'@` (одинарные кавычки: `$` в Dart-коде
  НЕ интерполируется). Закрывающий `'@` — строго в начале строки.
- Запись ТОЛЬКО без BOM: `[System.Text.UTF8Encoding]::new($false)` (иначе сломается
  кодогенерация — см. правило 5).
- Автоматически создавай директории, если их нет.
- В конце скрипта — готовые команды проверки одной строкой.
Ограничение: если содержимое файла содержит строку, начинающуюся с `'@`,
используй запись через `Set-Content`/`Add-Content` с `-Encoding UTF8` или разбей
на две команды. Такие случаи редки — проверяй перед выдачей.

🟢 АРХИТЕКТУРНЫЕ ПРАВИЛА (из DECISIONS.md, нарушать запрещено)

- Деньги — ТОЛЬКО IntColumn (копейки). double запрещён везде, кроме форматирования в UI.
- Даты — ТОЛЬКО UTC; локальное время через datetime(date, 'localtime') при выборке.
- Offline-First: любая запись создаётся с sync_status = 'pending'; sync асинхронный (WorkManager).
- E2E: чувствительные поля шифруются AES-256-GCM ДО отправки в Supabase; ключи только в SecureStorage.
- Clean Architecture: UI не знает о БД; цепочка UI → Provider → UseCase → Repository → Drift.
- Фильтры — ТОЛЬКО в SQL (WHERE), никогда в UI-слое. Хардкод строк/лимитов в UI запрещён.
- Все операции в try/catch с логированием. Один этап = один коммит.

✅ ЧЕКЛИСТ ПЕРЕД ГЕНЕРАЦИЕЙ КОДА (пройти по пунктам)

[ ] Контекст запрошен ОДНОЙ командой в файл (правило 17), файлы реально прочитаны
[ ] Изменения выдаются ОДНИМ скриптом на все файлы (правило 18), запись без BOM
[ ] Проверил версии пакетов в pubspec.yaml для используемых API
[ ] Проверил сигнатуры конструкторов (Select-String -Context)
[ ] Проверил существование файлов для всех импортов
[ ] Freezed-классы с abstract; после правок — принудительная регенерация
[ ] Прочитал enum-файлы, использую только существующие константы
[ ] intl: initializeDateFormatting() есть в main() для ru-локали
[ ] Нет дублей роутов/провайдеров/имён
[ ] Объяснил пользователю ожидаемое поведение и способ отмены действий
[ ] Деньги int, даты UTC, sync_status='pending', try/catch + логирование

🎯 ЭТАЛОННЫЙ ВОРКФЛОУ

Плохо: генерировать вслепую → просить файлы по одному → давать правки построчно → 10 итераций.
Хорошо:
1. Запросить ВЕСЬ нужный контекст одной командой в файл (правило 17).
2. Дождаться файл, прочитать реальные сигнатуры и структуру.
3. Выдать ОДИН скрипт, который создаёт/перезаписывает все файлы (правило 18).
4. В конце скрипта — готовые команды проверки одной строкой.
5. Одной фразой объяснить, что изменится на экране.
6. После чистого `flutter analyze` — команда коммита.

📌 ГЛАВНОЕ ПРАВИЛО
Если сомневаешься — СПРОСИ, а не предполагай.
Три вопроса дешевле десяти итераций отладки на устройстве пользователя.

Версия: 1.1 | Составлено по итогам Этапа 7 | Дополнено по итогам Этапа 9:
правила 16–18 (пользователь не программист; сбор контекста одной командой в файл;
применение изменений одним скриптом с записью без BOM). Хранить рядом с DECISIONS.md