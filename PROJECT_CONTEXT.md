# КОНТЕКСТ ПРОЕКТА: Budget Assistant v6.0

Навигатор по проекту. Обновлено: Этап 11 закрыт, Этап 12 в работе (микро-коммит 12.1).

## 📌 Иерархия документов

При работе над кодом и архитектурой руководствуйся следующей иерархией:

1. **DECISIONS.md** — Конституция проекта. Жёсткие ограничения (IntColumn, E2E, Offline-First, Riverpod, Drift). Приоритет №1.
2. **ТЗ 6.0** — Полное техническое задание в 7 Томах. Приоритет №2.
3. **PROJECT_CONTEXT.md** (этот файл) — Краткая сводка и навигатор. Приоритет №3.
4. **AI_RULES.md** — Правила взаимодействия с ИИ (сбор контекста одной командой, правки одним скриптом, UTF8 без BOM).

Примечание: все эволюционные изменения (Multi-group, Batch-импорт, ML Kit, Year in Review) полностью инкорпорированы в ТЗ 6.0. Отдельных документов для них не существует.

## 🏗 31 Зафиксированное Архитектурное Решение (Шпаргалка)

### Архитектура и Стек
- **Multi-group:** Связь Many-to-Many через memberships. Таблица users НЕ имеет space_id.
- **Секретность:** Режим секретности (подарки) — личная настройка в app_settings, а не глобальный свитч админа.
- **Импорт:** ПОЛНЫЙ ОТКАЗ от OpenAPI банков и API Т-Инвестиций. Только Batch-импорт (CSV/XLSX/PDF) + ручной ввод.
- **Платформа:** ТОЛЬКО Android.
- **БД:** schemaVersion стартует с 1 (чистая установка).

### Бизнес-логика и Данные
- **Деньги:** Все суммы в IntColumn (копейки). Проценты в базисных пунктах.
- **Переводы:** trusted_counterparties УДАЛЕНА. Автодетект переводов через алгоритмы (Сценарий А: Межбанк ±3 дня, Сценарий Б: СБП ±5 мин).
- **Конфликты:** sync_conflicts — гибрид (диалог для настроек/групп, Last-Write-Wins для транзакций).
- **Инвестиции:** Полностью исключены из P&L (пополнение = transfer). Портфель шифруется E2E.
- **Возвраты:** Логика is_refund УДАЛЕНА.
- **Кэшбэк:** Считаем только факт трат (NET-сумма с учётом возвратов). Обнуление недельного — Понедельник 00:00.
- **Уведомления:** 27 типов пушей. Все генерируются локально (WorkManager), сервер не имеет доступа к E2E-данным.

### UI/UX
- **Dashboard:** Кастомизируемый. FAB быстрых действий перенесён на Экран лога операций.
- **Privacy Matrix:** 3 режима (visible / partial / hidden). Централизованный PrivacyFormatter.
- **Empty States:** Обязательны для всех списков. Позитивные (нет долгов) с конфетти.

## 🛡 5 ЖЁСТКИХ ПРАВИЛ БИЗНЕС-ЛОГИКИ
1. **Сплит-чеки и Кэшбэк:** UseCase кэшбэка обязан делать JOIN с transaction_splits.
2. **Мультивалютность:** Суммы трат приводятся к валюте карты по курсу на дату транзакции.
3. **Переводы:** Жёсткий фильтр type != 'transfer' во всех расчётах P&L и лимитов.
4. **Возвраты:** NET = SUM(expense) - SUM(income с той же категорией).
5. **Часовые пояса:** Циклы в локальном часовом поясе (ПН 00:00 / 1-е число 00:00; в БД — UTC).

## 📚 Структура ТЗ 6.0 (Навигатор)
- **ТОМ 1:** Архитектура, Multi-group, Безопасность (E2E, HKDF).
- **ТОМ 2:** База Данных (Полная Схема ~35 таблиц, индексы, E2E-пометки).
- **ТОМ 3:** Безопасность (PIN, Биометрия, FLAG_SECURE, Shake/Face-down).
- **ТОМ 4:** Бизнес-Логика (P&L, Кэшбэк, Лимиты, Прогнозы, Мультивалютность).
- **ТОМ 5:** Уведомления (Матрица 27 пушей, WorkManager, Дайджесты).
- **ТОМ 6:** UI/UX (52+ экрана, Privacy Matrix, Empty States, Экспорт).
- **ТОМ 7:** Синхронизация, Импорт (Batch, OCR), Роутинг, Тестирование.

## 🚀 Регламент работы с AI
1. Один шаг = один коммит. Не пытаться сгенерировать весь проект за один промпт.
2. Сначала архитектура/псевдокод, потом реализация.
3. Сверяться с DECISIONS.md перед каждым архитектурным выбором.
4. Использовать PROJECT_CONTEXT.md как быструю шпаргалку, чтобы не перечитывать все 7 томов.
5. При противоречии в ТЗ 6.0 — приоритет у DECISIONS.md; внутри ТЗ — у более позднего Тома (Том 7 > Том 1).

## 🧭 Статус этапов
- Этапы 1–11: завершены (детали — в архиве «Закрыто»).
- **ЭТАП 12 — в работе** (микро-коммиты 12.1–12.9): БД + репозиторий → UseCases → интеграция с транзакциями → экраны → экспорт → Lottie.
- **Порядок следующих этапов (решение владельца):** 12 → 13 (Долги) → 14 (Напоминания) → 15 (Импорт: распознавание банковских PDF/CSV/XLSX, автопометка банка, автораскидывание категорий) → 16 (Чеки фото+файл, скриншоты кэшбэка через OCR) → далее по роадмапу.

## 🗄 Текущая схема БД
**schemaVersion = 9.**

- v4: transactions.is_large_expense; sync_locked_started_at / sync_locked_duration_ms (монотонные часы); синхронизационные служебные таблицы.
- v5 (Этап 9): budget_limits + уникальный индекс (space_id, user_id, category_id, year, month) + индексы; историческая мина миграции from < 2 удалена.
- v6 (Этап 10): exchange_rates (уникальный индекс from_currency + to_currency + date); cashback_matrix + индексы.
- v7/v8 (Этап 11): dashboard_widgets + уникальный индекс (user_id, widget_type); v8 — идемпотентная фикс-миграция.
- **v9 (Этап 12):** `savings_goals` (name/target_amount/current_amount/draft_amount — E2E; auto_reminder_enabled default TRUE; status default 'active'; completed_at nullable; sync_status) + `savings_goal_drafts` (локальная, без sync) + индексы: `idx_savings_goals_user_status(user_id, space_id, status, deadline, sync_status)`, `idx_savings_goals_sync_status`, `idx_savings_goal_drafts_user(user_id, updated_at)`.

Деньги — ТОЛЬКО IntColumn (копейки). Даты — ТОЛЬКО UTC.

## 🎯 Решения Этапа 12 (зафиксированы)
1. **auto_reminder_enabled default TRUE** — и в БД, и в форме. Решение владельца, закрывает противоречие спецификаций (6.3.16.14.9a говорит FALSE, 6.3.17.5 — TRUE).
2. **completed_at (nullable)** добавлен в savings_goals — одноразовое конфетти (триггер `completed_at IS NULL`) и дата «Завершена …» в архиве. Отклонение от ТОМ 2 §14.1, обосновано ТЗ экранов.
3. **Пополнение — всегда type='expense'** (и для виртуальных целей): 'transfer' из ТЗ 6.3.16.7 невозможен без счёта-приёмника; P&L уже исключает цели (фильтры Этапа 9).
4. **Изъятие:** type='income' + is_withdrawal=TRUE; валидация amount ≤ current_amount; из P&L исключено.
5. **Мультивалютная агрегация — в Dart** через цепочку курсов Этапа 10 (ExchangeRateRepository.getRate), не SQL-JOIN: единый фолбэк «последний известный курс» + предупреждение об устаревшем курсе.
6. **Экспорт аналитики: XLSX + CSV + PNG + PDF** (требование владельца: формат банковских сводок). В режиме hidden экспорт полностью заблокирован. Зависимости (excel, pdf, share_plus) — микро-коммит 12.8.
7. **Чекбокс «Регулярное пополнение»** отключён с подсказкой до Этапа 14 (нет таблицы recurring_transactions). WorkManager-логика автонапоминаний и дедлайн-пушей — Этап 18; флаг сохраняется уже сейчас.
8. **Архив:** инлайн-вкладка в SavingsGoalsScreen + роут-алиас /savings-goals/archive.
9. **Эмодзи хранится внутри name** (отдельной колонки в ТОМ 2 нет; форма держит эмодзи отдельно и склеивает при сохранении).
10. **Синхронизация:** строки пишутся с sync_status='pending'; в SyncTableSpec.defaultSpecs savings_goals НЕ добавляется до Этапа 25 (прецедент cashback_matrix/dashboard_widgets). E2E-поля для пейлоада: name, target_amount, current_amount, draft_amount (numeric).

## 💰 Справка по механикам Этапа 10
- Цепочка курса на дату D: 1) ручное исключение ровно на D; 2) официальный кэш (кросс через RUB) D..D-7; 3) ленивая загрузка ЦБ за D; 4) ручное исключение в окне 7 дней; 5) последний официальный курс любой даты; 6) null → понятная ошибка.
- exchange_rates НЕ синхронизируется в Supabase (локальный кэш по дизайну).
- Кэшбэк: NET = SUM(expense) − SUM(возвраты той же категории); is_split = true → только transaction_splits; type != transfer; циклы: неделя с ПН 00:00, месяц с 1-го 00:00 локального времени (в БД — UTC).
- Мультивалютность: операция в чужой валюте → amount пересчитывается в валюту счёта по цепочке, оригинал хранится в original_amount / original_currency.
- Матрица выгоды: строки — категории, колонки — карты (мои или семьи); лучшая карта = макс. процент (при равенстве — больший кэшбэк).
- Статусы: potential («Начислен») / approved («Подтверждён»), переключение тапом.

## 🖥 Справка по механикам Этапа 11 (Dashboard)
- Раскладка: dashboard_widgets; дефолт нового пользователя: balanceSummary, expenseFlowChart, activeCashbackCategories (видимые) + limitCards (скрыт); ensureDefaults при первом открытии.
- Кастомизация: long-press по виджету → BottomSheet (чекбоксы + drag-and-drop); сохранение обновляет sort_order/is_visible + sync_status='pending'.
- Синхронизация раскладки: до Этапа 25 dashboard_widgets НЕ в SyncTableSpec.defaultSpecs.
- SpaceSelector: PopupMenuButton в AppBar: пункт «Личное» + активные memberships.
- Privacy: тап по глазу циклит visible→partial→hidden; long-press — шторка с 3 режимами; маскирование ТОЛЬКО через PrivacyFormatter.
- Тема: тёмная по умолчанию (ThemeMode.dark в lib/app.dart); переключатель темы — Этап 21.
- Навигация: Drawer на Dashboard (Транзакции, Счета, Категории, Бюджет, Кэшбэк); раздел «Цели накопления» добавляется на Этапе 12.
- Lottie: 30 JSON в assets/animations/; замена без изменений кода.
- Источники виджетов: expense flow — SQL GROUP BY day за 7 дней (type='expense', savings_goal_id IS NULL, is_withdrawal=0, audit_status!='ignored', изоляция пространств); баланс — accounts; лимиты — budget_limits + GetEffectiveLimitUseCase.

## 👥 Семьи (Multi-group) — текущая реализация
- spaces + memberships; активная группа — currentSpaceIdProvider (core/providers/security_providers.dart).
- Лог транзакций: «Семейные» = ВСЕ транзакции активного пространства (включая мои); чип «Без моих» = без моих.
- Нет групп → empty-state с CTA «Создать группу»; группа есть, но семейных операций нет → CTA «Подключить мои транзакции».
- 2+ групп → селектор «Группа»; подпись сегмента «Семейные» = имя активной группы.
- Кэшбэк/матрица: источник «Мои карты / Карты семьи»; семейные счета = accounts с space_id активной группы; локально пусты до запуска синхронизации (Этап 25).
- Цели накопления: скоуп = мои цели + цели активного пространства (та же формула, что у лога транзакций).

## 📝 Отклонения от ТЗ (согласованы с владельцем)
1. Сегмент «Семейные» = все транзакции активного пространства; «только члены семьи» = чип «Без моих» (отклонение от ТЗ 6.3.2.3).
2. Циклы кэшбэка без flutter_timezone: локальный DateTime → toUtc.
3. Фильтр суммы в логе транзакций по ABS(amount).
4. is_large_expense НЕ применяется в лимитах по категориям.
5. approved — ручное подтверждение до банковской логики (Этапы 17/21).
6. «Лучшая карта» матрицы = макс. процент (при равенстве — больший кэшбэк).
7. Этап 11: 4 виджета вместо 6 — UpcomingRemindersPreview и OpenDebtsPreview отложены на Этапы 13/14.
8. Этап 11: BottomNavigation временно заменён Drawer; переход на нижнюю навигацию — на этапах полировки.
9. Этап 11: раскладка пишется с sync_status='pending', фактическая облачная синхронизация — после Этапа 25.
10. **Этап 12:** auto_reminder_enabled default TRUE (вопреки 6.3.16.14.9a).
11. **Этап 12:** колонка completed_at добавлена (нет в ТОМ 2 §14.1).
12. **Этап 12:** пополнение виртуальной цели — expense, а не transfer (6.3.16.7).
13. **Этап 12:** агрегация мультивалютных сумм в Dart через цепочку курсов Этапа 10, а не SQL-JOIN из 6.3.16.4/6.3.18.4.
14. **Этап 12:** экспорт расширен форматом PDF (нет в 6.3.18.13).

## ✅ Закрыто (архив — НЕ удалять, это история)
- Этапы 1–8: [x] проект, безопасность (AES-256-GCM, SecureStorage, HKDF), схема users/spaces/memberships/app_settings, Auth+онбординг+PIN+биометрия, счета+категории+ипотеки, транзакции CRUD+сторно-удаление, лог транзакций (пагинация/фильтры/меню), Sync Service (WorkManager + Supabase UPSERT + LWW-конфликты + монотонные часы), крупные траты, редактирование транзакций, фильтр суммы по ABS.
- Этап 9: [x] budget_limits + P&L с фундаментальными фильтрами, плавающие лимиты, матрица алертов, экран лимитов.
- Этап 10: [x] кэшбэк (5 правил), курсы ЦБ + исключения, мультивалютный UI, матрица выгоды, статусы, семейные скоупы, создание групп, PrivacyFormatter.
- Этап 11: [x] dashboard_widgets (v7+v8 фикс), DashboardScreen на `/`, 4 виджета, кастомизация (чекбоксы + drag-and-drop), SpaceSelector, PrivacyToggle, Empty States на Lottie, тёмная тема по умолчанию, зависимости lottie + fl_chart, Drawer-навигация.

## 📂 Открыто (долги и примечания)
Перенесено из прежних версий:
- [ ] l10n-фундамент: RU как основной (.arb), переводить все экраны за один раз — микро-коммит после Этапа 7
- [ ] initializeDateFormatting() + Intl.defaultLocale = 'ru' в main — временное решение до l10n
- [ ] excludeLargeExpenses: Dashboard-параметр готов, но не подключён (нет UI-фильтра); в лимитах НЕ применяется
- [ ] Ручное «Засекретить (Подарок)»: меняет только is_hidden_by_calendar; привязка к holidays и sync_locked — Этап 14/15
- [ ] Drag-and-drop сортировка счетов в UI — Этап 21
- [ ] Форма создания ипотеки — Этап 21
- [ ] Авто-создание системных счетов (is_system) — Этап 21
- [ ] enable_biometric_login не персистится в app_settings — Этап 21
- [ ] space_selector: заглушка — Этап 17/21
- [ ] onlyDebts-фильтр в логе транзакций — Этап 13
- [ ] workmanager 0.7.0 применяет KGP — мониторить релизы плагина

Добавлено после Этапов 9–11:
- [ ] Supabase: таблицы в облаке НЕ созданы (ошибки PGRST205 в логе — норма до Этапа 25: SQL-миграции + RLS)
- [ ] cashback_matrix не добавлена в SyncTableSpec.defaultSpecs — микро-коммит сразу после Этапа 25
- [ ] dashboard_widgets не добавлена в SyncTableSpec.defaultSpecs — микро-коммит сразу после Этапа 25
- [ ] exchange_rates не синхронизируется (по дизайну, локальный кэш)
- [ ] Счета участников семьи и их кэшбэк в матрице — после запуска синхронизации (Этап 25)
- [ ] approved — ручное подтверждение; авто-подтверждение от банка — Этапы 17/21
- [ ] Свитч use_historical_exchange_rate не реализован (поведение = всегда исторические курсы)
- [ ] Источники privacy-режимов (app_settings, shake, face-down, timeout) — Этапы 21/23
- [ ] Виджеты UpcomingRemindersPreview и OpenDebtsPreview на Dashboard — Этапы 13/14
- [ ] Lottie: заменить дизайнерские анимации (без изменений кода)
- [ ] BottomNavigation 5 табов (ТЗ 6.3.1) — переход с Drawer на этапах полировки

Добавлено на Этапе 12:
- [ ] savings_goals не добавлена в SyncTableSpec.defaultSpecs — микро-коммит сразу после Этапа 25 (вместе с cashback_matrix и dashboard_widgets); E2E-поля пейлоада: name, target_amount, current_amount, draft_amount
- [ ] Зависимости экспорта (excel, pdf, share_plus) — микро-коммит 12.8
- [ ] Lottie-анимации группы целей (confetti, celebration, empty_piggy, empty_box, search, pause_savings, broken_chart, empty_wallet, lonely_silhouette) — делает ассистент, микро-коммит 12.9
- [ ] Чекбокс «Регулярное пополнение» — активируется на Этапе 14 (таблица recurring_transactions)
- [ ] WorkManager: автонапоминания по целям + дедлайн-пуши — Этап 18
- [ ] Распознавание банковских PDF/CSV/XLSX, автопометка банка, автораскидывание категорий — Этап 15
- [ ] Чеки (фото+файл) + скриншоты кэшбэка через OCR — Этап 16

## Примечания
- Эмулятор: внутренний накопитель 10 ГБ, после wipe данные стабильны; полный flutter run безопасен, основной воркфлоу — hot reload.
- Два терминала: №1 — `flutter run` (r/R/q); №2 — скрипты правок, `flutter analyze`, git.